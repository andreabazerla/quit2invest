import React from 'react'

const SmokerStartDate = (props: { data: any; handleChange: any }) => {
    const {data,handleChange} = props

    return (
        <h1>Test</h1>
    )
}

export default SmokerStartDate